% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utlis.r
\name{split_formula}
\alias{split_formula}
\title{Split a Formula into Fixed and Random Parts}
\usage{
split_formula(fml)
}
\arguments{
\item{fml}{A one‐sided formula (class “formula”), e.g. `~ -1 + f_tl + (1 | f_tl:f_tk)`.}
}
\value{
A list with components:
\describe{
  \item{fixed}{A one‐sided formula containing only the fixed‐effects terms (e.g. `~ -1 + f_tl`).}
  \item{random}{A language object for the single random‐effect term (e.g. `(1 | f_tl:f_tk)`), or `NULL` if none.}
}
}
\description{
Separates a one‐sided formula into its fixed‐effects portion and single random‐effect term.
Only one random term is supported; if more than one “\|” is found, an error is thrown.
}
\examples{
f1 <- split_formula(~ -1 + f_tl + (1 | f_tl:f_tk))
# f1$fixed  == ~ -1 + f_tl
# f1$random == (1 | f_tl:f_tk)

f2 <- split_formula(~ 1 + f_tl)  # no random term
# f2$fixed  == ~ 1 + f_tl
# f2$random == NULL
}
